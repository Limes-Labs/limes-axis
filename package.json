{
  "name": "limes-axis",
  "private": true,
  "version": "0.0.0",
  "description": "The sovereign AI control plane for European operations.",
  "license": "Apache-2.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:web": "pnpm --filter @limes-axis/web dev",
    "build:web": "pnpm --filter @limes-axis/web build"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  }
}
