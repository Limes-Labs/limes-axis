{{- if .Values.networkPolicy.enabled -}}
{{- $egressMode := .Values.networkPolicy.egressMode | default "port_allowlist" -}}
{{- if eq $egressMode "local_only" -}}
{{- include "limes-axis.validateLocalOnly" . -}}
{{- end -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "limes-axis.fullname" . }}
  labels:
    {{- include "limes-axis.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "limes-axis.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: {{ .Values.api.service.port }}
        - protocol: TCP
          port: {{ .Values.web.service.port }}
  egress:
    {{- if eq $egressMode "local_only" }}
    {{- include "limes-axis.localOnlyEgress" . | nindent 4 }}
    {{- else }}
    - ports:
        - protocol: UDP
          port: {{ .Values.networkPolicy.dnsPort }}
        - protocol: TCP
          port: {{ .Values.networkPolicy.dnsPort }}
    {{- if eq $egressMode "port_allowlist" }}
    - ports:
        {{- range .Values.networkPolicy.allowedEgressPorts }}
        - protocol: TCP
          port: {{ . }}
        {{- end }}
    {{- else if eq $egressMode "restricted" }}
    {{- range .Values.networkPolicy.allowedEgressCidrs }}
    - to:
        - ipBlock:
            cidr: {{ . | quote }}
      ports:
        {{- range $.Values.networkPolicy.allowedEgressPorts }}
        - protocol: TCP
          port: {{ . }}
        {{- end }}
    {{- end }}
    {{- else if eq $egressMode "offline" }}
    # Offline mode intentionally renders no generic external egress rule, but
    # DNS stays unrestricted because the chart cannot infer the local
    # resolvers. Use egressMode: local_only for the strict #473 profile.
    {{- end }}
    {{- end }}
    {{- if .Values.tests.smoke.enabled }}
    - to:
        - podSelector:
            matchLabels:
              {{- include "limes-axis.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.api.service.port }}
        - protocol: TCP
          port: {{ .Values.web.service.port }}
    {{- end }}
{{- end }}
